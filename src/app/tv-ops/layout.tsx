import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'IC — Operations View',
};

export default function TvOpsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
