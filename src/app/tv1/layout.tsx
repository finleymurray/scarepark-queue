import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'IC — TV1: Mazes & Shows',
};

export default function TV1Layout({ children }: { children: React.ReactNode }) {
  return children;
}
