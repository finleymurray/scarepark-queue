import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'IC — TV2: Queue Times',
};

export default function TV2Layout({ children }: { children: React.ReactNode }) {
  return children;
}
