import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'IC — TV3.5: Fear Rating',
};

export default function TV35Layout({ children }: { children: React.ReactNode }) {
  return children;
}
