import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'IC — TV2.5: Queue Times',
};

export default function TV25Layout({ children }: { children: React.ReactNode }) {
  return children;
}
